import React from "react";
import { motion } from "framer-motion";

const Features = () => {
  return (
    <motion.section className="w-full flex items-center justify-center min-h-screen"></motion.section>
  );
};

export default Features;
